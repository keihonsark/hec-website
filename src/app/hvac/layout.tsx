import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "HVAC Installation & Repair Fresno | $0 Down | Home Energy Construction",
  description:
    "Energy-efficient HVAC systems built for Central Valley heat. $0 down financing, 228+ 5-star reviews. Free estimates. Call (559) 797-6081.",
};

export default function HVACLayout({ children }: { children: React.ReactNode }) {
  return children;
}
