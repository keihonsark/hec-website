import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Free Roof Cost Calculator | Instant Satellite Estimate | Home Energy Construction",
  description:
    "Get an instant roof replacement cost estimate using satellite data. See your roof area, pitch, and material costs. Free, no phone call required. Fresno, CA.",
};

export default function EstimateLayout({ children }: { children: React.ReactNode }) {
  return children;
}
