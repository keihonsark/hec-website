import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "The Complete Roof Transformation Package | $1,000 Off | Home Energy Construction",
  description:
    "Everything included: Owens Corning roof, $0 down 0% APR, permits, cleanup, lifetime warranty, solar R&R. Plus $1,000 off. Free satellite analysis. Fresno.",
};

export default function OfferLayout({ children }: { children: React.ReactNode }) {
  return children;
}
