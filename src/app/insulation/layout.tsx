import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Insulation Installation Fresno | Energy Savings | Home Energy Construction",
  description:
    "GreenFiber insulation upgrades that reduce energy bills up to 30%. Expert installation, financing available. Call (559) 797-6081.",
};

export default function InsulationLayout({ children }: { children: React.ReactNode }) {
  return children;
}
